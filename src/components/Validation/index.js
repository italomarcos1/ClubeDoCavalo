import React from 'react';

import { Header, Title } from './styles';

export default function Validation({ title }) {
  return (
    <>
      <Header>
        <Title>{title}</Title>
      </Header>
    </>
  );
}
